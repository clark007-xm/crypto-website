# 购票记录 RPC 限流修复

后续更新：本文保留初次限流修复的验证记录；当前实现已增加 IndexedDB 与逐场次增量索引，见 [纯前端索引优化验证](frontend-index-qa.md)。

日期：2026-09-17。范围：`/records` 全局购票记录读取；保留尚未提交的 PC 网络选择布局修复。

## 原因与改动

旧全局记录读取在场次目录完成后，同时启动购票、中奖两套全历史任务，每套并发 6 个请求。失败的批次会立即再试一次；多地址批次仍失败后，还会拆成每个合约单独查询。底层 ethers provider 自身也会重试 429。失败结果最终被过滤为空，UI 没有读取失败状态。

- 全局记录改用已有只读 RPC 客户端：串行间隔、真实超时、同链备用节点和失败节点冷却；移除该路径的旧并发和逐地址重试代码。
- 两类事件的第一项索引参数都是用户地址，使用 OR 事件主题合并查询。每个地址批次最多 25 个合约；地址批次全部成功才记录对应区块区间的完成度。
- 购票历史按最近到最早每批最多 6 个 9,000 区块窗口读取。失败停止；手动重试从失败区间继续。更早记录通过按钮继续读取，不把未扫描区间视为无记录。
- 页面展示目录同步阶段、扫描进度、历史未完成提示和失败提示。统计仅代表已读取记录。只有完整成功扫描后才展示无记录状态。
- 刷新同时检查新场次；相同场次集合保留当前购票扫描进度。钱包、链或场次集合变化时隔离记录，卸载或切换身份后停止后续批次。钱包事件仅保存在组件内存，不写入浏览器持久存储。
- 未连接钱包时不启动全历史目录或购票扫描。时间戳为辅助字段，读取失败不会抹掉已验证的购票事件。

## 验证

- `npm run test:chain-loading`：25 项通过，包括 RPC 限流/换节点、超时、分页、失败批次不推进进度、取消和页面错误/部分/完整空态渲染。
- `npx tsc --noEmit --incremental false`：通过。
- `npm run build`：通过；本地 3101 已重启为最新生产构建。
- 浏览器实际 `/records` 未连接钱包状态：展示连接提示，无加载转圈；捕获 4 次 `eth_blockNumber` 健康检查，0 次 `eth_getLogs` 历史扫描。
- 页面渲染测试使用受控状态，RPC 测试使用模拟响应，不等同于真实钱包端到端验收。
- 不操作钱包连接、签名、购票或提现；没有读取或记录用户本地购票密钥。

## 限制

场次目录首次仍需分段补齐，并显示进度；购票历史未读完时需要继续读取更早记录。公共 RPC 仍可能限流，修复保证受控停止、提示和续读，不承诺节点永不失败。详情页的单场记录与其他资金活动读取路径未纳入本次修改。
