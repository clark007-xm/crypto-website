# One Tap 链上读取调整与验证

日期：2026-09-16。范围：公共奖池目录、首页状态、按需开奖记录；保留现有两套主题。

## 原因与改动

- 旧目录从配置的部署区块开始，按 9,000 区块串行扫描；检查时一次完整扫描约 436 次事件请求。首页奖池和开奖记录独立扫描，公共 RPC 限流后会叠加 SDK 长重试。
- 新目录按链、Factory、部署区块隔离，共享进行中的扫描。仅把公开创建事件和成功扫描区间保存到本地；刷新读取新增区块并回查末尾 64 个区块。
- 首页从最近区块向前每批最多扫描 6 个窗口，逐步显示已查到的场次；稀疏部署首次没有记录时，通过历史代码查询定位部署附近的两个窗口。这只是补充查找，不把中间未查询区域判定为空。
- 状态读取先处理 12 个场次；更早场次通过按钮继续读取。开奖记录在用户点击后挂载，复用同一目录，开奖事件每批最多读取 6 个窗口。
- 独立只读 RPC 客户端检查链号、串行限速、单次 HTTP 5 秒超时、单次逻辑读取最多 20 秒节点尝试预算，失败节点冷却 30 秒。错误明确展示，并支持重试；失败区间不会推进历史扫描进度。
- 未改钱包签名、购票、提现或管理员身份逻辑。

## 验证

- `npm run test:chain-loading`：14 项通过，覆盖扫描顺序、渐进发布、请求共享、持久化、增量与重组、失败续读、缓存隔离、存储不可用、429 换节点、超时、错误网络、稀疏部署提示和共享订阅取消。
- `npx tsc --noEmit --incremental false`：通过。
- `npm run build`：通过。
- 浏览器冷启动，生产构建 `127.0.0.1:3101`：8 次事件查询（最近 6 个窗口和部署附近 2 个窗口），11 次代码查询，9 次状态/代币查询，显示 3 个真实场次。捕获的首个至最后一个 RPC 请求发起时间跨度约 14.5 秒；这不是严格的首个奖池绘制耗时。
- 浏览器缓存刷新，开发构建 `127.0.0.1:3102`：只发出 1 次目录事件查询，范围 11717895–11717959，共 65 个区块；已有目录成功恢复，无重复全历史扫描。
- 最终生产构建重启后刷新 `127.0.0.1:3101`：同样只有 1 次目录事件查询，范围 11717918–11717982，共 65 个区块；3 个奖池恢复显示。9 次状态/代币读取、1 次链号检查，以及包含原有节点健康检查的 5 次区块高度读取仍会发生。
- 开奖记录默认保持“读取开奖记录”入口。点击后目录继续补全，首页已有奖池仍显示，扫描进度从 72,000 增至 423,015 个区块。未将本次测试描述为完整历史开奖记录验收。
- 受控屏蔽当前链的 RPC 后：页面显示节点不可用和重试入口，没有误报“暂无奖池”。验证后已撤销浏览器网络屏蔽。
- 全程未连接钱包、签名或发送交易。

## 边界

首次完整历史仍需补齐，公共 RPC 延迟和限流仍会影响速度。页面明确显示历史未读完；稀疏部署快速找到的场次不代表所有最新场次。只有成功查询的区间计入完成度。缓存不是服务端索引，清空浏览器存储或换设备后会重新开始。

本次没有新增后端索引服务，也没有实现 Factory.admin() 管理员测试链权限。以上验证在提交前完成。

浏览器截图保存在 `/Users/mimi/.codex/visualizations/2026/09/16/01a0aab4-083a-73d2-afd9-83b032db39b7/one-tap-build/chain-loading-production.png` 和同目录 `chain-read-unavailable.png`。
