# 纯前端链上读取优化

日期：2026-09-17。范围：首页奖池和 `/records` 全局购票历史。沿用公共 RPC，不增加数据库服务、后台索引器或 Vercel Function。保留之前未提交的 PC 网络设置布局和限流修复。

## 实现

- IndexedDB 保存公开的场次事件、成功扫描区间、购票/中奖事件、代币元数据、区块时间和奖池状态。缓存失败或不可用时仍可实时查链；不存储密钥、签名或 RPC 凭证。
- 目录按页发现，记录随已知场次逐步读取；每个场次保存独立进度，新增场次不使其他场次重扫。每轮最多查询六个窗口，每窗口最多 9,000 区块。未读完的历史需要点击继续读取，统计标为部分数据。
- 购票从对应场次的创建区块开始；键隔离链、Factory、部署起点、钱包、场次地址及创建区块哈希。刷新只补查新增区块和 64 区块重组校验区间。未成功的区间不记作已完成。
- 奖池状态和事件先从浏览器缓存恢复，再后台核验。显示最近读取时间、缓存/同步提示；节点失败时保留已读数据，不能把读取失败显示成“没有记录”。
- 通过创建交易回执及前一区块无代码核实当前 Sepolia Factory 部署在 **10,662,082**，比原配置 **7,800,000** 晚 **2,862,082** 个区块。证明及精确链/合约匹配表在 `lib/contracts/verified-deployments.ts`。未知合约保持原配置；明确配置的更晚起点不被覆盖。
- IndexedDB 条目有效期 30 天，代币元数据复用期 24 小时。内存降级最多 100 个键，日志缓存每项限制 2 MB；清理浏览器数据或换设备后仍需重新同步。

## 验证

- `npm run test:chain-loading`：37 项通过。覆盖限流、节点切换、超时、异步缓存恢复、增量续读、创建区块边界、钱包/网络隔离、缓存损坏、存储不可用、重组替换、逐场次进度、状态金额序列化和错误/部分记录页面状态。
- `npx tsc --noEmit --incremental false`：通过。
- `npm run build`：通过。3101 运行最终生产构建。
- 独立浏览器来源首次访问：8 次 `eth_getLogs`（6 个最新窗口 + 2 个部署附近窗口），9 次状态调用，3 个场次逐步展示；部署窗口从 **10,662,082** 开始，没有历史代码探测请求。
- 真实首页重载恢复 3 个已读场次；一次暖启动采样仅 **1 次 `eth_getLogs`**，区间 **11,721,412–11,721,477**。另外有 5 次区块高度调用（含健康检查）、1 次链 ID 核验、7 次状态调用。
- 阻断全部配置的 Sepolia RPC 后重载，仍显示 3 个缓存场次及最近读取时间，并显示节点不可用/缓存数据提示；无无限加载转圈，无横向溢出。验证后已恢复正常网络。
- 390×844 移动端记录页：连接提示可见，无转圈、无横向溢出；未连接时仅 4 次区块高度健康检查，0 次日志扫描。
- 以上仅描述本次观测，不是固定延迟承诺。钱包相关查询由受控事件测试验证；浏览器没有连接真实钱包，也没有签名、购票或提现。

## 边界

初次访问、换设备、缓存被浏览器回收以及很久未同步时，仍受公共节点速度及历史范围影响。未扫描区间不会宣称已核验；64 区块以外的深重组不在自动重读窗口内。缓存仅改善展示与读取，不替代交易时的链上状态校验。详情页的单场记录与其他资金活动路径不在本次范围。静态目录快照暂未引入。
