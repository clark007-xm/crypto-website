# 场次详情请求风暴修复

日期：2026-09-17。接续资金页修复，覆盖详情状态、个人票数、购票/中奖记录以及场次 Treasury 概览。

## 原因与处理

- 详情仍使用独立的 ethers 读取路径：状态字段并发调用，失败被转换为零值/false；个人购票和中奖记录自动扫描全部历史。购票组件与 Hook 各自触发首次刷新，重复发起扫描。这些路径没有接入此前资金页的限速与取消机制。
- 状态、票数和 Treasury 概览改用共享只读客户端。字段顺序读取，失败即停止；相同链、节点、场次/账户的消费者共享进行中的请求与短期结果。倒计时重渲染不重新发起请求，错误后需要手动重试。
- 购票与个人中奖事件合并过滤，默认只读 3 个 9,000 区块窗口；公共中奖结果独立按页读取，仅在正常结算后启用。成功区间写入本机索引，继续读取从历史游标推进；未读完明确显示部分历史，不冒充没有记录。
- 页面卸载、钱包/网络切换取消在途请求和后续窗口。字段读取、日志和辅助时间查询都使用 AbortSignal。
- 删除组件额外的首次刷新。场次读取失败显示错误与重试；Treasury 未核实数据显示未知。购票入口、弹窗及领取面板等待有效读取结果，交易函数、签名、ABI 和合约逻辑未改。

## 验证

- `npm run test:chain-loading`：60 项通过。新增详情读取失败、代币精度失败、字段取消、重复消费者去重、30 次重渲染、分页续读、卸载取消、钱包切换以及错误 UI 回归。
- `npx tsc --noEmit --incremental false`：通过。
- `npm run build`：通过。3101 本地服务已重启为新生产构建。
- 对截图 Tesla 场次执行真实 Sepolia 只读查询：19 次字段/代币调用加 1 次链校验成功，代币精度为 6，场次未结算。未连接真实钱包或发送交易。
- 新生产构建的浏览器验证使用独立 localhost 标签与 `tests/fixtures/session-detail-browser.js`：
  - 正常：状态每个字段仅调用一次；购票和资金流水各 3 次日志查询，总计 6 次后停止。页面显示已扫描 27,000 区块和继续读取按钮，后续观察计数不变。
  - 日志全部 429：合计 4 次节点尝试后停止，两块历史均显示限流提示和缓存状态，无无限加载或自动重试。
  - 状态全部 429：首个字段最多 4 次节点尝试后停止，日志请求为 0；显示限流和“重新读取”，不显示场次不存在或假定的开奖状态。
  - 离页：第一个日志请求进行中点击移动导航“奖池”，abort 从 0 变为 1；后续旧详情日志请求为 0，观察计数保持不变。
  - 390×844：错误页面无横向溢出。
- 模拟钱包标记和两条模拟场次索引已清理，隔离标签关闭，视口恢复；用户 127.0.0.1 标签没有注入测试夹具。

## 边界

公共 RPC 仍可能限流。本次保证读取有界、失败可见、离页停止；历史较久的场次可能需要手动继续读取。未执行真实钱包购票、领取或签名交易联调。

浏览器夹具仅供独立 localhost 文档开发测试，在 `/session/<模拟地址>?detailQa=normal|logs429|state429|cancel` 执行。它替换文档的只读 RPC 与模拟钱包，拒绝签名和交易；完成后只清理模拟地址的索引与连接标记并关闭标签，不在真实钱包页面执行。
