# 资金页限流与离页取消修复

日期：2026-09-17。范围：资金活动、账户 Treasury 余额及共用的合伙人身份读取；保留之前未提交的奖池、购票历史和 PC 设置布局修改。

## 原因与处理

- 原资金活动按 9 类事件 × 全历史窗口生成任务，6 路并发。24/16 条 limit 仅在全部查询结束后截取展示；离开页面无清理，批次失败直接重试，底层 SDK 还有 429 重试。
- 改用与奖池共用的受控只读客户端。按索引参数位置把钱包事件合为 2 组（按场次查询为 1 组）。一页最多 3 个 9,000 区块窗口，即正常情况下最多 6 次钱包日志查询；失败停止，已完成区间可续读，历史未读完明确标记。
- 页面拥有独立 AbortController。卸载、禁用、切换网络/钱包时终止其活动请求；请求队列出队前和故障转移前检查取消状态。辅助区块时间查询也受同一生命周期控制。
- 余额和合伙人身份只读调用共享同一限速客户端；相同账户、目标合约和方法共享结果与进行中的请求。多个消费者中的最后一个退出才取消共享请求，单个页面退出不会误取消其他仍在显示的消费者。
- 失败不会改写成真实零余额/空流水。资金页和钱包菜单对未核实余额显示未知；错误后可手动重试。资金页提现输入基于有效读取状态启用，交易函数、ABI、签名和发送流程没有改动。
- 流水缓存隔离链、Treasury、钱包/场次与扫描起点；Factory 的创建区块不能作为 Treasury 的已证实部署区块。全局资金历史暂以 0 作为保守下界，按页向前读取；单场历史可用已知场次创建区块。

## 验证

- `npm run test:chain-loading`：50 项通过。其中新增资金分页、事件主题位置、失败不推进游标、取消当前/排队请求、共享余额去重、最后订阅者退出、旧结果隔离、实际 Hook effect 清理和错误 UI 测试。
- `npx tsc --noEmit --incremental false`：通过。
- `npm run build`：通过。
- 生产构建浏览器受控测试使用 `tests/fixtures/treasury-browser.js`：模拟只读账户与 RPC，无真实钱包、签名、交易或外部 RPC 流水请求。
  - 取消：第一个 Treasury 日志请求挂起时点击首页导航，abort 计数从 0 变为 1；首页稳定后，旧 Treasury 新日志请求计数保持 0。首页自己的目录查询可以继续。
  - 全节点 429：4 次有限的节点尝试后停止，展示“节点请求受限，已尝试备用节点。请稍后重试。”，没有显示无资金活动。
  - 正常页：6 次日志调用后停止，扫描 27,000 区块，显示部分历史及继续读取按钮。
  - 390×844 移动端：无横向溢出，分页结束后无历史加载骨架。
- 模拟连接标记和模拟账户的流水缓存已清理，临时浏览器标签已关闭；测试夹具没有注入用户原来的 127.0.0.1 标签。

## 浏览器夹具使用边界

仅在独立 localhost 测试标签中、`/treasury?rpcQa=cancel|normal|rate-limit` 上用开发者运行环境执行夹具。它替换该文档的 fetch 与注入钱包，拒绝所有签名/交易方法。验证完成后移除测试 origin 的 `wallet_connected` 标记及该模拟账户的 Treasury 缓存并关闭标签。不要用于真实钱包标签，也不打包进产品入口。

公共节点仍可能返回 429；本次保证工作有界、离页可取消、失败可见。真实用户钱包联调未执行。详情页其他旧状态读取函数不在此次资金列表与账户读取改动内。
